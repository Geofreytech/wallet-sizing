# Corporate Wallet Sizing Backend
**Version:** 1.0 (Sprint 1)
**Architecture:** DDD / Hexagonal

## Setup
1. Clone the repo.
2. Ensure MySQL (XAMPP) is running.
3. Create a database named `wallet_sizing_dev`.
4. Run: `./mvnw spring-boot:run -Dspring-boot.run.profiles=dev`